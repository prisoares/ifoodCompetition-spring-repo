CREATE TABLE ifoodCompetition.restaurant (
    id INTEGER NOT NULL AUTO_INCREMENT PRIMARY KEY,
    name VARCHAR(100) NOT NULL,
    description VARCHAR(100) NOT NULL,
    address VARCHAR(100) NOT NULL,
    cnpj VARCHAR(100) NOT NULL
);